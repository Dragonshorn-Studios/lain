[Unit]
After=laind.service
Wants=laind.service

[Service]
# cloudflared remains an independent system service. Lain only updates the
# existing Tunnel ingress configuration through the Cloudflare API.
Restart=always
RestartSec=5
